{
  "frontend/src/index.css": "Global styles with Cheetah 1.0 dark theme using OKLCH color tokens — charcoal/black backgrounds, yellow-orange cheetah accent colors, Inter/JetBrains Mono fonts, and custom utility classes for glow effects and scrollbars.",
  "frontend/tailwind.config.js": "Tailwind config extended with cheetah color palette tokens (orange, yellow, dark, surface variants), Inter/JetBrains Mono font families, and custom animations for the browser UI.",
  "frontend/src/hooks/useTabManager.ts": "Tab state management hook with updated `TabState` interface that includes `searchResults` (array of `GoogleSearchResult`) and `searchQuery` fields to support Google Custom Search result display alongside proxy content.",
  "frontend/src/hooks/useQueries.ts": "React Query mutations for backend proxy and search requests — `useSearchRequest` now parses the Google Custom Search API JSON response and returns structured `GoogleSearchResponse` with `items` (title, link, snippet, displayLink) and `searchInformation`.",
  "frontend/src/hooks/useCloaking.ts": "About:blank cloaking hook that opens a new browser window showing about:blank in the address bar while embedding the full Cheetah app in an iframe inside it.",
  "frontend/src/components/TabBar.tsx": "Browser-style tab bar component displaying all open tabs with active state highlighting (orange bottom border), close buttons, loading indicators, and a new-tab button.",
  "frontend/src/components/AddressBar.tsx": "Smart address bar that detects URL vs search query input, shows appropriate icons, handles Enter/Escape keys, and provides clear/submit buttons while focused.",
  "frontend/src/components/CloakButton.tsx": "Falcon emoji button with tooltip that triggers the about:blank cloaking feature to disguise the app.",
  "frontend/src/components/ProxyContent.tsx": "Renders proxied HTML content in a sandboxed iframe with link/form interception to keep navigation within the proxy, plus loading, error, and new-tab page states.",
  "frontend/src/components/BrowserLayout.tsx": "Main browser chrome layout that now conditionally renders `SearchResults` for search queries (non-URL inputs) and `ProxyContent` for URL navigation, with DuckDuckGo references removed and reload logic updated to re-run searches by query.",
  "frontend/src/App.tsx": "Root app component rendering BrowserLayout with a dark-themed Sonner toaster.",
  "frontend/src/components/SearchResults.tsx": "New component that renders Google Custom Search API results in the Cheetah dark theme — displays a search header with query and total results count, then a list of result items each showing the display URL, clickable title, and snippet."
}