{
  "frontend/src/index.css": "Global styles with Cheetah dark OKLCH theme tokens plus new HomePage purple theme tokens (home-bg, home-glow, home-title-gradient, nav-drawer-bg, etc.) and corresponding utility classes for the purple home page aesthetic.",
  "frontend/tailwind.config.js": "Tailwind config extended with cheetah color palette tokens (orange, yellow, dark, surface variants), Inter/JetBrains Mono font families, and custom animations for the browser UI.",
  "frontend/src/hooks/useTabManager.ts": "Tab state management hook; fixes `closeTab` to correctly create a fresh tab and set its ID as active when the last tab is closed, preventing a stale `activeId` pointing to a non-existent tab.",
  "frontend/src/hooks/useQueries.ts": "React Query mutations for `useProxyRequest` (calls `actor.proxyUrl`, detects content type from the raw response, and returns a structured `{ body, contentType, statusCode }` object; handles base64 image detection and data URL conversion) and `useSearchRequest` (DuckDuckGo HTML search parser returning `GoogleSearchResponse`).",
  "frontend/src/hooks/useCloaking.ts": "Exports `CloakMode` type and `useCloaking` hook with a `cloak(mode)` function that opens a full-screen iframe popup disguised as `about:blank`, `https://error`, or `123456789101112` in the address bar using `history.replaceState`.",
  "frontend/src/components/TabBar.tsx": "Browser-style tab bar component displaying all open tabs with active state highlighting (orange bottom border), close buttons, loading indicators, and a new-tab button.",
  "frontend/src/components/AddressBar.tsx": "Smart address bar synced to the active tab's URL; submits on Enter or arrow button click, resets cleanly on blur, shows search/globe/loading icons contextually, and avoids showing internal `search:` prefixes.",
  "frontend/src/components/CloakButton.tsx": "Falcon cloak button with an attached dropdown mode selector (about:blank / https://error / 123456789101112); the selected mode persists in local state and is passed to `useCloaking`'s `cloak` function on click, styled with the cheetah dark/orange theme.",
  "frontend/src/components/ProxyContent.tsx": "Renders proxied content in a sandboxed iframe using `srcdoc`; processes HTML by rewriting relative URLs to absolute, removing CSP meta tags, injecting a `<base>` tag, and injecting a navigation interceptor script that uses `postMessage` to route link clicks and form submissions through the parent's `onNavigate` handler. Handles loading, error, image, and new-tab states.",
  "frontend/src/components/BrowserLayout.tsx": "Main browser chrome orchestrating tabs, proxy navigation, search, games, and the new HomePage default view; integrates HomeIconButton and NavigationDrawer for global page navigation, showing HomePage when the active tab is blank/new.",
  "frontend/src/App.tsx": "Root app component rendering BrowserLayout with a dark-themed Sonner toaster.",
  "frontend/src/components/SearchResults.tsx": "Renders Google Custom Search results with display URL, clickable title, and snippet; the click handler guards against empty `result.link` before calling `onNavigate`.",
  "frontend/index.html": "HTML entry point with the page title changed to \"Study Tools\", a neutral meta description for educational content, and a `noindex, nofollow` robots meta tag to prevent crawlers from categorizing the site.",
  "frontend/src/components/GamesPage.tsx": "New Games page component displaying a responsive grid of three game site cards (Math Playground, Hood Math, Unblocked Games) with thumbnails, descriptions, tags, and click-to-navigate buttons that use the existing proxy navigation mechanism, styled with the Cheetah dark/orange theme.",
  "frontend/src/components/ClockWidget.tsx": "A live clock widget that displays the current time (HH:MM:SS) and short day/date (e.g., \"Mon Jun 10\") using setInterval, styled with Cheetah orange accent and muted-foreground tokens.",
  "frontend/src/components/HomePage.tsx": "Full-screen dark purple radial gradient home page with a live 12-hour clock at the top, large gradient-text \"CHEETAH\" branding in the center, a muted tagline, and a version/Discord info line at the bottom.",
  "frontend/src/components/NavigationDrawer.tsx": "Slide-in left drawer with backdrop overlay listing navigation links (Home, Browser, Games, Search) with icons and descriptions; accepts isOpen/onClose/onNavigate props and closes on Escape or backdrop click.",
  "frontend/src/components/HomeIconButton.tsx": "Fixed bottom-left home icon button styled with the purple home theme that triggers the navigation drawer when clicked."
}